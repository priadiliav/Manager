import { CollapsibleSection } from "../wrappers/CollapsibleSection";

export const MetricsConfigurationForm = () => {
    return (
        <CollapsibleSection title="Metrics Configuration" noCard>
            <div>Metrics Configuration Form Content</div>
        </CollapsibleSection>
    );
}